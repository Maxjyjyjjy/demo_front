# Vite 会注入以 VITE_ 开头的变量。修改后请重启 `npm run dev`。
#
# 后端 API 根地址（不要末尾斜杠）。
# 留空 = 不连后端，登录与作业走本地 mock（适合纯前端调试）。
# 接后端时取消下一行注释并改为你的服务地址，例如：

# 本地典型示例（按需启用）
# VITE_API_BASE_URL=http://127.0.0.1:8080  https://maxschedule.zeabur.app

VITE_API_BASE_URL=http://127.0.0.1:8080
